<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), #url(https://blog.ubaldi.com/wp-content/uploads/2019/05/cocktails-blender.jpg);">
  <div class="container">
    <h1><%= @cocktail.name %></h1>
    <% @cocktail.doses.each do |dose| %>
    <div class="card-category">
    <p> <%= dose.description%> - <%= dose.ingredient.name%> </p>
    <%= link_to "Supprimer", dose_path(dose), method: :delete, class: "btn btn-danger btn-sm", data: { confirm: "Are you sure ?" } %>
    </div>
    <% end %>
  </div>
</div>

<div class="container mt-4">
  <%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary mb-4" %>
  <%= link_to "Back to list", cocktails_path, class: "btn btn-primary mb-4" %>
</div>
